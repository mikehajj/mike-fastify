"use strict";

/**
 * Define the list of drivers that should be auto-loaded upon server start or when triggering a command
 * each entry contains a static configuration that is supplied to the driver regardless of the environment that the
 * driver is running inside and regardless of who is calling this driver.
 * This configuration is access inside the driver through the options property
 * Ex: this.option.XXX
 */
module.exports = {

};