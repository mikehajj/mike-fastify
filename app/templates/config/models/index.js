"use strict";

/**
 * Define the list of models that should be auto-loaded upon server start or when triggering a command
 * each entry contains a static configuration that is supplied to the model regardless of the environment that the
 * model is running inside and regardless of who is calling this driver.
 * This configuration is access inside the model through the options property
 * Ex: this.option.XXX
 */
module.exports = {

};