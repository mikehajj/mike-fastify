"use strict";

/**
 * Define the list of modules that should be auto-loaded upon server start or when triggering a command
 * each entry contains a static configuration that is supplied to the module regardless of the environment that the
 * module is running inside and regardless of who is calling this driver.
 * This configuration is access inside the module through the options property
 * Ex: this.option.XXX
 */
module.exports = {

};