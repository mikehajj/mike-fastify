"use strict";

const config = {
    errors: {
        '190': 'Unable to build needed url for redis to connect.',
        "191": "Invalid Config. please send a valid config to Redis's constructor",
    }
};

module.exports = config;