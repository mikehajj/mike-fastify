"use strict";

const config = {
    errors: {
        '190': 'Unable to build needed url for elastic search to connect.',
        '191': 'index name is required.',
        "192": "Invalid DB Config. please send a valid DB config to ElasticSearch's constructor",
    }
};

module.exports = config;