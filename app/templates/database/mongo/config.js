"use strict";

const config = {
    errors: {
        '190': 'Unable to build needed url for mongo to connect.',
        '191': 'collection name is required.',
        "192": "Invalid DB Config. please send a valid DB config to MongoDriver's constructor",
    }
};

module.exports = config;