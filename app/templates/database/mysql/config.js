"use strict";

const config = {
    errors: {
        '190': 'Unable to build needed url for mysql to connect.',
        '191': 'table name is required.',
        "192": "Invalid DB Config. please send a valid DB config to MySQLDriver's constructor",
    }
};

module.exports = config;